# ApnaVaidya Environment Configuration
# Set the backend URL if running on a remote cloud host (e.g. Render, Railway, AWS)
# Leave empty for automatic local discovery (probes localhost:8080 and localhost:8081)
VITE_API_URL=
